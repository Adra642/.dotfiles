# tmux main config
# Keep this file minimal and source module files from conf.d.

# Core tmux behavior
source-file ~/.config/tmux/conf.d/general.conf

# Status bar layout
source-file ~/.config/tmux/conf.d/status.conf

# Keybindings
source-file ~/.config/tmux/conf.d/keybindings.conf
